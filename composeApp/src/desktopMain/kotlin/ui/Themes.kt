package ui

import androidx.compose.material.darkColors
import androidx.compose.ui.graphics.Color

val skyBlueTheme = darkColors(
	primary = Color(0xFF87CEEB), // Sky blue color
	primaryVariant = Color(0xFF65B3D5), // Darker sky blue color
	secondary = Color(0xFF333399), // Lighter deep blue color
	secondaryVariant = Color(0xFF1A1A66), // Darker deep blue color
	background = Color(0xFF000033), // Deep blue color
	surface = Color(0xFF202040), // Darker surface color
	error = Color(0xFFFF0000), // Red color for errors
	onPrimary = Color(0xFFFFFFFF), // White color on primary color
	onSecondary = Color(0xFFFFFFFF), // White color on secondary color
	onBackground = Color(0xFFFFFFFF), // White color on background color
	onSurface = Color(0xFFFFFFFF), // White color on surface color
	onError = Color(0xFFFFFFFF) // White color on error color
)

val purpleTheme = darkColors(
	primary = Color(0xFF800080), // Purple color
	primaryVariant = Color(0xFF570057), // Darker purple color
	secondary = Color(0xFF333399), // Lighter deep blue color
	secondaryVariant = Color(0xFF1A1A66), // Darker deep blue color
	background = Color(0xFF000033), // Deep blue color
	surface = Color(0xFF202040), // Darker surface color
	error = Color(0xFFFF0000), // Red color for errors
	onPrimary = Color(0xFFFFFFFF), // White color on primary color
	onSecondary = Color(0xFFFFFFFF), // White color on secondary color
	onBackground = Color(0xFFFFFFFF), // White color on background color
	onSurface = Color(0xFFFFFFFF), // White color on surface color
	onError = Color(0xFFFFFFFF) // White color on error color
)

val tealTheme = darkColors(
	primary = Color(0xFF008080), // Teal color
	primaryVariant = Color(0xFF005757), // Darker teal color
	secondary = Color(0xFF333399), // Lighter deep blue color
	secondaryVariant = Color(0xFF1A1A66), // Darker deep blue color
	background = Color(0xFF000033), // Deep blue color
	surface = Color(0xFF202040), // Darker surface color
	error = Color(0xFFFF0000), // Red color for errors
	onPrimary = Color(0xFFFFFFFF), // White color on primary color
	onSecondary = Color(0xFFFFFFFF), // White color on secondary color
	onBackground = Color(0xFFFFFFFF), // White color on background color
	onSurface = Color(0xFFFFFFFF), // White color on surface color
	onError = Color(0xFFFFFFFF) // White color on error color
)

val deepBlueTheme = darkColors(
	primary = Color(0xFF0000FF), // Blue color
	primaryVariant = Color(0xFF000099), // Darker blue color
	secondary = Color(0xFF333399), // Lighter deep blue color
	secondaryVariant = Color(0xFF1A1A66), // Darker deep blue color
	background = Color(0xFF000033), // Deep blue color
	surface = Color(0xFF202040), // Darker surface color
	error = Color(0xFFFF0000), // Red color for errors
	onPrimary = Color(0xFFFFFFFF), // White color on primary color
	onSecondary = Color(0xFFFFFFFF), // White color on secondary color
	onBackground = Color(0xFFFFFFFF), // White color on background color
	onSurface = Color(0xFFFFFFFF), // White color on surface color
	onError = Color(0xFFFFFFFF) // White color on error color
)

val matteGreenTheme = darkColors(
	primary = Color(0xFF3C493F), // Matte green color
	primaryVariant = Color(0xFF2B3729), // Darker matte green color
	secondary = Color(0xFF7E8A7C), // Lighter matte green color
	secondaryVariant = Color(0xFF5F6E60), // Darker matte green color
	background = Color(0xFF202124), // Dark grey color
	surface = Color(0xFF2B2B2B), // Darker grey color
	error = Color(0xFFB00020), // Dark red color for errors
	onPrimary = Color(0xFFE0E0E0), // Light grey color on primary color
	onSecondary = Color(0xFFE0E0E0), // Light grey color on secondary color
	onBackground = Color(0xFFE0E0E0), // Light grey color on background color
	onSurface = Color(0xFFE0E0E0), // Light grey color on surface color
	onError = Color(0xFFFFFFFF) // White color on error color
)

val matteBlueTheme = darkColors(
	primary = Color(0xFF3B4252), // Matte blue color
	primaryVariant = Color(0xFF2B303B), // Darker matte blue color
	secondary = Color(0xFF5E81AC), // Lighter matte blue color
	secondaryVariant = Color(0xFF4C6E88), // Darker matte blue color
	background = Color(0xFF202124), // Dark grey color
	surface = Color(0xFF2B2B2B), // Darker grey color
	error = Color(0xFFB00020), // Dark red color for errors
	onPrimary = Color(0xFFE0E0E0), // Light grey color on primary color
	onSecondary = Color(0xFFE0E0E0), // Light grey color on secondary color
	onBackground = Color(0xFFE0E0E0), // Light grey color on background color
	onSurface = Color(0xFFE0E0E0), // Light grey color on surface color
	onError = Color(0xFFFFFFFF) // White color on error color
)

val matteRedTheme = darkColors(
	primary = Color(0xFF4E3D42), // Matte red color
	primaryVariant = Color(0xFF3B2D31), // Darker matte red color
	secondary = Color(0xFF8F5C5C), // Lighter matte red color
	secondaryVariant = Color(0xFF6E4646), // Darker matte red color
	background = Color(0xFF202124), // Dark grey color
	surface = Color(0xFF2B2B2B), // Darker grey color
	error = Color(0xFFB00020), // Dark red color for errors
	onPrimary = Color(0xFFE0E0E0), // Light grey color on primary color
	onSecondary = Color(0xFFE0E0E0), // Light grey color on secondary color
	onBackground = Color(0xFFE0E0E0), // Light grey color on background color
	onSurface = Color(0xFFE0E0E0), // Light grey color on surface color
	onError = Color(0xFFFFFFFF) // White color on error color
)